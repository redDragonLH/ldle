/**
 * 1009. 十进制整数的反码
 *
 * 每个非负整数 N 都有其二进制表示。例如， 5 可以被表示为二进制 "101"，11 可以用二进制 "1011" 表示，依此类推。注意，除 N = 0 外，任何二进制表示中都不含前导零。
 * 二进制的反码表示是将每个 1 改为 0 且每个 0 变为 1。例如，二进制数 "101" 的二进制反码为 "010"。
 * 给你一个十进制数 N，请你返回其二进制表示的反码所对应的十进制整数。
 */

/**
 * @param {number} n
 * @return {number}
 */
/**
 * 返回十进制整数 n 的二进制反码对应的十进制数
 * 例如 n = 5 (二进制 101)，反码为 010，对应十进制 2
 *
 * @param {number} n
 * @return {number}
 */
var bitwiseComplement = function (n) {
  // 特殊情况：0 的反码是 1
  if (n === 0) return 1;

  // mask 用于生成与 n 等长的全 1 二进制数
  let mask = 1;
  // mask 不断左移，直到大于 n
  while (mask <= n) {
    mask = mask << 1; // mask 左移一位，相当于乘以 2
  }
  // mask - 1 得到与 n 等长的全 1 二进制数
  // 用这个数减去 n，相当于对 n 取反
  return mask - 1 - n;
};
/**
 * 这个原理很有意思,获取等位长度的全1数,然后减去原数,全一数减去原数就相当于对原数取反了
 * 例如 n = 5 (二进制 101)，mask 最终会变成 1000 (十进制 8)，mask - 1 就是 111 (十进制 7)，7 - 5 就是 2，正好是反码的十进制数
 * 时间复杂度 O(log n)，空间复杂度 O(1)
 
 * 执行用时：0 ms, 在所有 JavaScript 提交中击败了100.00%的用户
 * 内存消耗：54.14 MB, 在所有 JavaScript 提交中击败了50.00%的用户
 */